---
const { googleDocsURL } = Astro.props;

const bulletinID = googleDocsURL
  ? googleDocsURL.match(/(?<=d\/).+?(?=\/)/gm)
  : "";

const src = `https://drive.google.com/file/d/${bulletinID}/preview`;
---

{bulletinID && <iframe src={src} width="100%" height="480" allow="autoplay" />}
