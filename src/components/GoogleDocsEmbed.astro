---
const { bulletin_link } = Astro.props;

const bulletinID = bulletin_link
  ? bulletin_link.match(/(?<=d\/).+?(?=\/)/gm)
  : "";

const src = `https://drive.google.com/file/d/${bulletinID}/preview`;
---

{bulletinID && <iframe src={src} width="640" height="480" allow="autoplay" />}
